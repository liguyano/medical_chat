# 患者登录取消请求与护士监控栏位修复计划

## 需求边界

- 附件截图仅作为护士监控页栏位移动的视觉参考；红框、箭头和截图中的示例数据不作为额外业务指令。
- 患者端修复 `component-unmounted` 请求取消导致的 Console Error，不改变患者身份校验和任务查询接口语义。
- 护士端将“结构化答案”“风险与宣教事件”（以及同属结果信息的 AI 评估总结）移入左侧任务进度区域下方；右侧只保留“本轮 AI 质评”。

## 实施步骤

### 1. 代码分析与方案确认

- [X] 1.1 确认 Console Error 来自前端 AbortController 的自定义 `component-unmounted` reason，而非后端接口失败。
- [X] 1.2 确认护士监控页当前 DOM 将结果卡片和 AI 质评卡片放在同一个右侧容器。
- [X] 1.3 与需求方确认取消请求和三栏重排方案。

### 2. 患者端请求取消修复

- [X] 2.1 将组件卸载取消统一为标准 AbortError，保留超时与主动取消的错误分类。
- [X] 2.2 检查患者登录后任务查询的 hydration/路由竞态，避免未完成身份恢复时误跳转。
- [X] 2.3 增加 HTTP 客户端和患者任务加载相关测试，验证取消不产生业务错误提示。

### 3. 护士监控布局修复

- [X] 3.1 重排桌面三栏：左侧进度与结果卡片，中间对话回放，右侧仅 AI 质评。
- [X] 3.2 保持 AI 消息选中、质评保存/更新、移动端自动定位和结果卡片滚动能力。
- [X] 3.3 用真实浏览器检查桌面和 390px 视图无横向溢出、右栏不再出现结果卡片。

### 4. 验证与交付

- [X] 4.1 运行前端测试、lint、typecheck、生产构建。
- [X] 4.2 使用 `backend/.venv` 运行后端 unit-test（154 项通过；系统默认 Python 的全量收集因缺少 `alembic.config` 中断）。
- [X] 4.3 完成代码 review，更新中文 review 文档。
- [X] 4.4 提交修复分支、合并本地 `main` 并删除修复分支。
