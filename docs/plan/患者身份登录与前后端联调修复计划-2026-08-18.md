# 患者身份登录与前后端联调修复计划

## 1. 需求目标

- [X] 患者端使用身份证号和手机号登录。
- [X] 仅允许存在“在院”住院记录的患者登录。
- [X] 登录成功后查询并展示患者本人护理任务。
- [X] 任务编号仅用于任务审计、医护端定位和任务展示，不作为登录凭证。
- [X] 修复 API 模式下页面卸载时产生的 `AbortError` 控制台报错。
- [X] 在 `deploy/Install.md` 写明联调账号、患者登录方式和完整操作路径。

## 2. 后端

- [X] 新增患者登录请求、响应和会话 DTO。
- [X] 新增患者登录接口，校验身份证号、手机号和在院状态。
- [X] 使用 Redis 保存患者登录会话，并通过 HttpOnly Cookie 传递会话。
- [X] 新增当前患者信息与本人任务查询接口。
- [X] 补齐演示患者身份数据和一键种子命令。
- [X] 更新后端 API 文档。

## 3. 前端

- [X] API 模式患者端改为身份登录，不读取本地 Mock 任务作为登录依据。
- [X] 登录成功后加载当前患者任务并写入任务 Store。
- [X] 患者任务中心支持刷新后重新查询本人任务。
- [X] Mock 模式继续保留任务编号与证件后四位演示流程。
- [X] 页面卸载时静默处理主动取消的请求。

## 4. 文档与验证

- [X] 更新 `deploy/Install.md`。
- [X] 更新前后端联调文档。
- [X] 运行后端检查、前端 lint、typecheck 和 build。
- [X] 使用浏览器验证护士创建任务、患者登录、任务进入和护士监控流程。
