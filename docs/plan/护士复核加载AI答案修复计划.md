# 护士复核加载 AI 答案修复计划

## 1. 问题与方案

- [X] 1.1 确认护士复核页只读取 Zustand 缓存，未主动加载 API 模式的 AI 抽取快照。
- [X] 1.2 复用实时监控页的 `getDialogueSnapshot()`，进入复核页时刷新会话、结构化答案和事件。

## 2. 验证

- [X] 2.1 运行前端 TypeScript 检查。
- [ ] 2.2 在 API 模式确认 AI 答案可显示、护士手填答案仍可保存。
