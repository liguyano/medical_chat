# 患者对话输入框状态恢复修复计划

## 1. 问题确认

- [X] 1.1 核对任务 `109` 与会话 `SESS-418B276E41A1` 的 PostgreSQL 状态。
- [X] 1.2 核对 Redis 首问输出快照与 SSE 事件状态。
- [X] 1.3 确认患者端未发起 `POST /api/dialog/message`，输入在前端被禁用。
- [X] 1.4 定位 Zustand 持久化旧 `pending` 会话、页面跳过后端快照刷新以及首问完成事件未同步 `active` 的状态恢复缺陷。

## 2. 回归测试

- [X] 2.1 增加首问完成事件将会话切换为 `active` 并结束流式状态的测试。
- [X] 2.2 增加 Chat Store 持久化配置测试，确保 `streamingTaskId` 不跨页面恢复。
- [X] 2.3 增加患者对话快照刷新策略测试，覆盖已有旧会话仍需从后端刷新。

## 3. 前端修复

- [X] 3.1 调整患者对话页加载逻辑，API 模式下进入页面必须刷新后端会话快照。
- [X] 3.2 调整首问完成事件处理，同步把 `pending` 会话切换为 `active`。
- [X] 3.3 调整 Chat Store 持久化范围，排除 `streamingTaskId` 等瞬时状态。
- [X] 3.4 避免快照刷新过程中重复请求或覆盖正在进行的本地发送状态。

## 4. 验证

- [X] 4.1 运行前端相关 Vitest 回归测试。
- [X] 4.2 运行 `pnpm lint` 与 `pnpm typecheck`。
- [X] 4.3 运行 `pnpm build`。
- [X] 4.4 使用真实浏览器复现任务 `109` 的旧 `pending` 状态，验证首问显示后输入框可编辑。

## 5. 审查与交付

- [X] 5.1 审查状态恢复、SSE 幂等和页面卸载边界。
- [X] 5.2 更新相关 `AGENTS.md` 架构约定。
- [X] 5.3 按 Git 规范提交、合并回本地 `main` 并删除修复分支。
