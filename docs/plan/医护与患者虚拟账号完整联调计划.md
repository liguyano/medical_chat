# 医护与患者虚拟账号完整联调计划

## 需求目标

按照 `deploy/Install.md` 的本地联调流程，补充多组可重复初始化的医护与患者虚拟账号：

- 医护账号进入数据库，密码以 bcrypt 哈希保存；
- 医护端在 API 模式使用后端账号登录、退出和会话校验；
- 患者演示数据补齐身份证密文，保证身份证号+手机号登录可用；
- 扩充患者演示账号，并同步安装文档、接口文档与测试。

## 开发步骤

### 1. 现状与方案确认

- [X] 1.1 阅读 `deploy/Install.md`、现有患者种子、前端登录和数据库模型。
- [X] 1.2 确认医护侧当前仅为前端 Mock，确定新增 `staff_account` 与 Redis 会话方案。
- [X] 1.3 创建功能分支并保留现有主分支基线。

### 2. 后端账号模型与数据库迁移

- [X] 2.1 新增 `StaffAccount` ORM 模型、统一导出及账号唯一/状态索引。
- [X] 2.2 新增 Alembic 迁移，创建 `staff_account` 表并支持回滚。
- [X] 2.3 更新 ORM 元数据与迁移集成测试的表数量、版本断言。
- [X] 2.4 在数据库事实来源文档补充医护账号表设计。

### 3. 后端认证服务与种子数据

- [X] 3.1 新增密码哈希/校验工具和医护会话安全配置。
- [X] 3.2 新增医护账号 Schema、Service、Redis 会话及登录/退出/当前用户 API。
- [X] 3.3 新增幂等 `StaffSeeder`，写入多组医护演示账号且不落明文密码。
- [X] 3.4 为患者种子增加身份证号密文写入，并扩充患者演示数据。
- [X] 3.5 将医护与患者种子接入 `seed_demo`，保持重复执行幂等。

### 4. 业务接口鉴权边界

- [X] 4.1 医护查询患者、量表、任务、质量评价和监控 SSE 接口要求有效医护会话。
- [X] 4.2 患者消息提交和患者 SSE 校验当前患者会话与会话所属患者一致。
- [X] 4.3 对话历史/字段抽取支持已登录患者或医护读取，避免两端联调互相影响。
- [X] 4.4 任务创建时校验负责医护账号存在且启用。

### 5. 前端联调适配

- [X] 5.1 增加医护登录 DTO、Repository 接口及 API/Mock 实现。
- [X] 5.2 医护登录页按运行模式调用对应 Repository，展示认证失败信息并更新演示账号提示。
- [X] 5.3 医护端退出时清理后端会话；保留现有 sessionStorage 的双标签页隔离。
- [X] 5.4 增加 DTO 映射与 Repository 登录测试。

### 6. 文档、测试与交付

- [X] 6.1 更新 `deploy/Install.md` 的医护/患者演示账号清单和联调步骤。
- [X] 6.2 更新后端 API 文档、后端架构约定和数据库设计文档。
- [X] 6.3 执行后端单元/迁移集成测试与前端 lint/typecheck/test。
- [X] 6.4 完成代码审查记录，修复问题后复测。
- [X] 6.5 提交中文摘要 commit，合并回本地 `main` 并删除功能分支。
