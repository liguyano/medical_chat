# 医护端历史任务跨浏览器恢复修复计划

日期：2026-08-19

## 1. 根因确认

- [X] 1.1 确认 `/nurse/monitor` 当前只读取 Zustand `sessionStorage`，没有登录后调用后端任务列表接口。
- [X] 1.2 确认后端仅有创建任务和按 ID 查询接口，没有按当前医护账号查询任务的 GET 接口。
- [X] 1.3 确认任务 109 属于持久化数据库记录，换浏览器后前端没有历史 Store 快照因此不可见。

## 2. 后端任务列表接口

- [X] 2.1 增加当前医护账号任务列表查询 Schema/Service，按 `assigned_nurse_id` 和未删除条件查询，按更新时间倒序。
- [X] 2.2 增加 `GET /api/tasks` 医护鉴权接口，复用完整任务 DTO 与真实进度/呼叫状态。
- [X] 2.3 增加后端单元测试，覆盖按医护隔离、历史任务返回和无任务返回。

## 3. 前端登录后任务同步

- [X] 3.1 为 `CareRepository` 增加 `listMyTasks` API 实现，保留 Mock 实现。
- [X] 3.2 医护认证成功后刷新后端任务列表并替换本地 Store，清除跨浏览器旧快照造成的假数据。
- [X] 3.3 监控中心、任务管理、工作台共享同一后端任务快照；详情页仍支持直接按任务 ID加载。
- [X] 3.4 增加仓储和任务同步回归测试。

## 4. 验证与合并

- [X] 4.1 运行后端 unit-test、前端 Vitest、TypeScript、Lint、Build。
- [X] 4.2 用任务 109 验证新浏览器登录后能看到历史任务和详情。
- [X] 4.3 更新 review 文档，提交中文 Git 记录。
- [ ] 4.4 合并回本地 `main` 并删除修复分支，不推送远程。
