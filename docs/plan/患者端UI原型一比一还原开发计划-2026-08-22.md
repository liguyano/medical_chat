# 患者端 UI 原型一比一还原开发计划

## 一、需求与边界

- [X] 1.1 对照 `docs/ui/患者端` 四张新版原型图、素材交付清单和部件规格。
- [X] 1.2 确认以 390px 移动端视口为主要一比一验收基准，桌面端使用居中的移动端内容容器。
- [X] 1.3 确认保留动态患者、任务、进度、状态和接口数据，不把原型中的静态演示值写死到业务逻辑。
- [X] 1.4 锁定后端接入边界：不修改 API DTO、Repository 接口、SSE、WebSocket、Store 数据结构和生产访问路径。

## 二、患者端视觉基础

- [X] 2.1 将交付的 SVG、WebP 和状态素材复制到 `frontend/public/assets/patient`。
- [X] 2.2 在全局样式中接入患者端颜色、圆角、阴影、字体、触控区和安全区令牌。
- [X] 2.3 重构 `PatientLayout`，实现移动端画布、顶部栏和三栏底部导航。
- [X] 2.4 新增患者端复用组件，包括图标、品牌标识、进度、步骤条、语音 Orb 和空/错/完成状态。

## 三、首页、任务与身份场景

- [X] 3.1 按原型重构患者身份核验页，保留 Mock/API 两种核验流程及家属协助逻辑。
- [X] 3.2 按原型重构住院服务首页，展示问候、床位、任务进度、呼叫护士和快捷入口。
- [X] 3.3 按原型重构护理任务中心，保留患者任务刷新、准备状态、异常和完成分组。
- [X] 3.4 按原型重构任务详情页，展示评估流程、内容范围、安全说明和动态开始/继续操作。
- [X] 3.5 按原型重构住院助手，保留生活问答限制、快捷问题和文字交互。

## 四、评估、知情同意与完成场景

- [X] 4.1 保留 AI 对话事件、消息、工具卡、语音 WebSocket 和任务进度逻辑，仅重构页面结构和视觉。
- [X] 4.2 实现语音 Orb、权限/连接/监听/转写确认/思考/播报/异常/文字保底等状态视觉。
- [X] 4.3 按原型重构知情同意条款、播放、理解确认、护士解释和签名区域。
- [X] 4.4 按原型重构评估完成页，使用交付插画并保留动态任务和知情同意结果。
- [X] 4.5 统一传统问卷页面的患者端视觉，不改变问卷加载、草稿和正式提交接口。

## 五、测试与验收

- [X] 5.1 补充患者端视觉组件或页面行为单元测试。
- [X] 5.2 执行 `pnpm test`、`pnpm lint`、`pnpm typecheck` 和 `pnpm build`。
- [X] 5.3 使用真实浏览器检查 390px 手机视图和桌面端视图。
- [X] 5.4 检查控制台无错误，并走通身份核验、任务详情、AI 对话、知情同意和完成页关键路径。
- [X] 5.5 检查变更未修改后端接入层、生产同源路径和接口数据结构。
- [X] 5.6 对原型中的每个状态标注“已接真实数据 / 前端派生或模拟 / 待后端提供”，保留明确对接位置。

## 六、Review、Debugger 与交付

- [X] 6.1 对视觉一致性、可访问性、动态数据边界和前后端联调边界进行代码审查。
- [X] 6.2 根据审查结果修复问题并重新执行检查。
- [X] 6.3 在 `docs/review` 输出 UI 复现结果与后端待办清单，记录字段、事件和前端预留接入位置。
- [X] 6.4 停止本次验证启动的前后端进程，确认没有后台残留占用端口。
- [X] 6.5 使用中文规范提交信息提交功能分支。
- [X] 6.6 合并回本地 `main` 并删除已完成的功能分支。

## 七、真实浏览器复核修复

- [X] 7.1 在 Mock 模式启动前端，逐页复核身份核验、首页、任务、任务详情、AI 对话、知情同意、完成页和住院助手。
- [X] 7.2 使用 390px 与 1440px 视口检查移动画布宽度、底部导航定位和长页面滚动。
- [X] 7.3 修复移动端滚动条 gutter 挤窄原型画布、导航选中态过重、顶部标题字号偏小和语音 Orb 尺寸/表情偏差的问题。
- [X] 7.4 保持 API、Repository、SSE、WebSocket、Store 和生产访问路径不变，并记录 API 模式字段差异。
- [X] 7.5 重新执行测试、Lint、类型检查、生产构建并停止本轮验证进程。
