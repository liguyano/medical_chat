# 患者对话页 ChunkLoadError 修复计划

日期：2026-08-18

## 1. 需求与现状确认

- [X] 1.1 阅读历史联调记录和 `backend/docs/api.md`，确认最后未完成问题为患者对话页运行时崩溃。
- [X] 1.2 确认错误发生于前端 JavaScript chunk 加载阶段，尚无证据表明任务 69、后端 API、SSE 或真实模型先失败。
- [X] 1.3 从干净的 `main` 创建分支 `fix/frontend-chunk-load`。
- [X] 1.4 制定本修复计划，后续按实际证据更新完成状态。

## 2. 故障复现与根因定位

- [X] 2.1 使用生产模式访问 `/patient/dialogue/69`，记录浏览器控制台错误和失败网络请求。
- [X] 2.2 核对失败 chunk 的 HTTP 状态、页面构建标识和服务器 `.next` 构建标识。
- [X] 2.3 核对当前 Next.js 进程、启动方式和 `.next/dev`、生产构建产物的生成时间。
- [X] 2.4 排除 Service Worker、浏览器旧页面缓存、重复前端进程和构建期间覆盖产物等因素。
- [X] 2.5 形成明确根因；如果重新构建并重启即可恢复，不修改无关业务源码。

## 3. 最小修复

- [X] 3.1 停止本项目当前前端进程，避免旧进程继续提供过期资源清单。
- [X] 3.2 由 `next build` 重新生成生产输出；因安全策略禁止脚本递归删除，本次不手工清理源码之外的目录。
- [X] 3.3 使用锁定依赖重新执行生产构建并以唯一的 `next start` 进程启动。
- [X] 3.4 新构建、重启后错误不再复现，因此不修改无关页面、动态导入或路由源码。
- [X] 3.5 更新 `deploy/Install.md` 和前端模块说明，明确生产构建与重启规则。

## 4. 端到端验证

- [X] 4.1 在带构建查询参数的新页面中打开 `/patient/dialogue/69`，确认不再出现 `ChunkLoadError`；当前构建引用的 13 个静态资源均返回 HTTP 200。
- [X] 4.2 通过真实患者认证会话验证任务详情、本人任务列表、对话历史和字段抽取接口。
- [X] 4.3 通过真实患者认证会话验证患者 SSE 返回 HTTP 200、`text/event-stream` 且可读取首个事件帧。
- [X] 4.4 检查浏览器控制台无未处理异常；默认视口和 390px 视口页面外壳均可加载，认证态业务数据通过 API 会话验证。

## 5. 质量检查与收尾

- [X] 5.1 执行前端测试（14 项）、类型检查、Lint 和生产构建。
- [X] 5.2 审查本次差异，确认未混入后端、Agent 或其他无关修改。
- [X] 5.3 将根因、修复方式、验证结果和环境状态记录到计划和 Bug 文档。
- [X] 5.4 测试通过后按 Git 规范提交、合并回本地 `main` 并删除功能分支。
