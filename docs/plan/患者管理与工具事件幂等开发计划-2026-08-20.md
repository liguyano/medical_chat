# 患者管理与工具事件幂等开发计划

## 1. 数据设计与接口契约

- [X] 1.1 更新患者与住院记录数据库事实来源，明确新增字段、敏感信息和历史快照边界。
- [X] 1.2 扩展 `patient`、`patient_encounter` ORM，并创建 Alembic 迁移。
- [X] 1.3 定义医护端患者列表、详情、新增和编辑请求/响应契约。
- [X] 1.4 定义工具调用身份契约：患者主动调用编号、Agent 原始 `call_id` 和领域事件关联。

## 2. 后端患者管理

- [X] 2.1 实现患者编号、住院过程编号生成与身份证加密/脱敏。
- [X] 2.2 实现患者列表搜索、科室/病区/状态筛选和任务摘要。
- [X] 2.3 实现患者主档与当前住院记录的一体化新增、详情和编辑。
- [X] 2.4 增加字段校验、唯一性校验、医护身份校验及事务回滚测试。

## 3. 医护端患者管理

- [X] 3.1 扩展前端患者类型、DTO 映射和 API/Mock 仓储接口。
- [X] 3.2 重构患者列表，接入真实 API、筛选、状态摘要和空态/错误态。
- [X] 3.3 新增共享患者表单及患者新增页面。
- [X] 3.4 重构患者详情并新增编辑页面，展示联系、住院、临床安全和护理任务信息。
- [X] 3.5 增加 DTO、仓储和关键交互测试。

## 4. 工具事件重复展示修复

- [X] 4.1 患者主动呼叫携带稳定客户端调用编号，后端保证同一调用只生成一个事件。
- [X] 4.2 文字与语音 Agent 透传原始 `call_id`，相同调用重复交付时复用领域事件。
- [X] 4.3 保留不同 `call_id` 的模型重复调用，不按工具名称或参数限制模型行为。
- [X] 4.4 统一 HTTP 返回、SSE 和数据库快照的事件身份，前端按业务事件身份更新同一组件。
- [X] 4.5 对任务 109 一类缺少调用编号的旧患者事件做严格的历史展示兼容。
- [X] 4.6 增加患者按钮、相同 `call_id` 重放、不同 `call_id` 重复调用的回归测试。

## 5. 验证、审查与交付

- [X] 5.1 执行 Alembic 升级并验证现有演示数据兼容。
- [X] 5.2 执行后端单元、集成测试和代码质量检查。
- [X] 5.3 执行前端单元测试、lint、typecheck 和生产构建。
- [X] 5.4 使用真实浏览器验证患者新增/编辑、任务 109 历史展示，并以自动化回归验证呼叫护士单次展示。
- [X] 5.5 验证桌面端和 390px 移动端布局，检查浏览器控制台。
- [X] 5.6 完成代码审查、调试回归并更新模块 `AGENTS.md`。
- [X] 5.7 合并回本地 `main`、删除功能分支并停止本次联调前后端进程。
