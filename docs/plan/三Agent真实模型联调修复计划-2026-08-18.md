# 三 Agent 真实模型联调修复计划

## 1. 运行链路核对

- [X] 1.1 核对 `schedule_agent`、`dialog_agent`、`extraction_agent` 的模型绑定。
- [X] 1.2 验证 `DASHSCOPE_API_KEY`、API 地址和真实 HTTP 请求。
- [X] 1.3 检查 Celery Worker 注册、活跃任务和重复进程。
- [X] 1.4 定位 Extraction 输出截断与 Dialog 静态回退问题。

## 2. 模型配置修复

- [X] 2.1 支持向 OpenAI 兼容接口透传 `enable_thinking`。
- [X] 2.2 TextChatEngine 透传温度、输出长度、重试和供应商参数。
- [X] 2.3 为真实模型调用增加不含密钥和患者隐私的运行日志。

## 3. Agent 行为修复

- [X] 3.1 Dialog 首问通过真实模型生成。
- [X] 3.2 Dialog 后续问句始终优先采用真实模型输出。
- [X] 3.3 模型失败时发布错误并触发任务重试，禁止静默回退量表原文。
- [X] 3.4 Extraction 关闭思考模式，避免结构化结果被推理 token 截断。
- [X] 3.5 Schedule 增加真实模型调用成功和失败日志。

## 4. 验证与文档

- [X] 4.1 执行模型工厂、Dialog、Schedule、Extraction 单元测试。
- [X] 4.2 使用 `qwen3.5-flash` 完成真实普通响应和结构化响应验证。
- [X] 4.3 检查三个 Worker 的任务注册和队列路由。
- [X] 4.4 更新 `deploy/Install.md` 的模型配置与 Worker 重启说明。
