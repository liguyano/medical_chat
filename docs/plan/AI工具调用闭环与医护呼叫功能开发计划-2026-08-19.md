# AI 工具调用闭环与医护呼叫功能开发计划

日期：2026-08-19
分支：`feat/agent-tool-interaction`

## 1. 需求与现状审计

- [X] 核查 Dialog Agent 原生工具调用、工具执行、结果回填和多轮模型调用链路。
- [X] 核查 Redis Stream、SSE 领域事件及患者端/医护端事件消费链路。
- [X] 核查宣教、知情同意数据模型、接口和现有前端组件的可复用范围。

## 2. 工具调用闭环修复

- [X] 确保模型原生 `tool_calls` 被执行，工具调用过程不作为患者最终回答。
- [X] 确保工具结果回填模型上下文，并发布稳定的结构化业务事件。
- [X] 为模型漏调工具增加非阻塞安全兜底，并保留工具失败后的正常解释。

## 3. 医学宣教工具

- [X] 返回稳定的宣教材料结构（标题、原文、展示文本、播报文本、确认要求等）。
- [X] 将宣教工具结果转换为 `education_triggered` SSE 事件。
- [X] 患者端增加原文展示卡片、确认操作和自动播报。
- [X] 增加播报状态处理，避免重复播报。

## 4. 知情同意工具

- [X] 返回知情同意文档、版本、条款和签署要求。
- [X] 将同意工具结果转换为 `consent_triggered` SSE 事件。
- [X] 患者端在对话内显示条款签署卡片并接入签名板。
- [X] 提交签名后发布/消费同意状态事件，并保持幂等。

## 5. 呼叫医护工具

- [X] 新增 `request_nurse_assistance` 工具，支持原因、人工操作和紧急程度。
- [X] 将工具结果转换为 `handoff_requested` SSE 事件并持久化交互事件与任务状态。
- [X] 患者端显示呼叫中状态和护士处理结果。
- [X] 医护端全局提醒与监控详情显示患者、床位、呼叫原因和请求操作，并支持处理。

## 6. 测试与验证

- [X] 补充 Agent 工具调用闭环单元测试。
- [X] 补充后端 SSE/事件映射测试。
- [X] 补充前端事件解析和 Store 测试。
- [X] 运行后端单元/集成测试、前端 lint/typecheck/build。
- [X] 使用浏览器验证宣教、签署和呼叫完整链路，检查控制台无错误。

## 7. Review、Debug 与 Git 收尾

- [X] 进行代码审查，修复发现的问题（包括模型工具旁白过滤、事件幂等和旧进程验收）。
- [X] 更新相关架构/模块文档。
- [ ] 按规范提交中文摘要 commit。
- [ ] 合并到本地 `main`，确认工作区干净后删除功能分支。
