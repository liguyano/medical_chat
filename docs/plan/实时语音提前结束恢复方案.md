# 实时语音提前结束恢复方案

## 基线

- 开发基线：`fix/qwen-structured-thinking@de269ab`
- 功能分支：`fix/voice-premature-close-recovery`
- 基线差异：该稳定版本尚未引入后续分支中的 `dialog_question_service / report_question_choice` 候选题门禁，因此恢复选题直接使用“Schedule Task-todo 顺序 ∩ 结构化 remaining_question_ids”，一次只选择一题。

## 目标

Qwen Realtime 已经把一轮语音完整播给患者后，如果回复明确宣告“评估完成/结束”，系统再做事后核验。若当前患者回答尚在 Extraction Agent 处理中，先异步等待该消息被处理；处理完成后以结构化评估进度为唯一事实来源。未完成则自动纠正并补问一题，已完成则维持现有结束流程。

该方案不做输出前审核，不增加实时音频首包延迟。

## 设计边界

- [X] 评估是否完成只相信 `assessment_progress_service` 的有效结构化答案。
- [X] 结束语检测只判断 AI 回复有没有明确的结束意图，不判断业务是否完成。
- [X] 单独“谢谢/感谢配合”不视为结束，降低误判。
- [X] 同一个 Qwen Realtime 连接继续保留原 conversation history。
- [X] 恢复模式重新构建 instructions，只暴露当前一条未完成题，不重新下发整张后续问题表。
- [X] 恢复模式持续到 `remaining_question_ids` 清空；每答完一题重新等待 Extraction，再选择下一条 remaining。
- [X] 恢复期间患者重新开口只取消旧推进任务，不得退回完整 Task-todo。
- [X] 当前恢复题的自动语音回复必须先 `response.done`，再创建下一条恢复提问，避免并发双响应。
- [X] 真正 SessionEnd 仍走原有“Extraction 完成 + 可见 response.done”完成屏障。
- [X] 恢复响应本身不再次进入提前结束检测，避免递归恢复。

## 开发步骤

### 1. 基线与接口确认
- [X] 1.1 从 `fix/qwen-structured-thinking@de269ab` 创建独立功能分支。
- [X] 1.2 核对 `voice_gateway`、`assessment_progress_service`、Schedule Task-todo 与 Qwen `session.update/response.create`。
- [X] 1.3 确认该基线没有 question-choice 候选/冷却机制，恢复选题按 Task-todo 原顺序确定。

### 2. 提前结束检测
- [X] 2.1 新增 `voice_turn_guard.py`。
- [X] 2.2 第一版采用可测试的明确结束语规则，识别“评估完成/结束/今天到这里/问题问完”等语义。
- [X] 2.3 增加否定语义保护，例如“评估还没有完成，我们继续下一项”不得误触发。
- [X] 2.4 增加 Guard 单元测试。

### 3. 等待 Extraction 后判断真实进度
- [X] 3.1 结束候选只在完整 `response.done` 后触发。
- [X] 3.2 从 PostgreSQL 找到最新患者正式消息。
- [X] 3.3 异步等待 `extraction_agent:state:{session}` 将该消息记录到 `processed_message_ids`。
- [X] 3.4 等待期间不阻塞 Qwen 上游事件消费；患者重新发言则放弃恢复。
- [X] 3.5 Extraction 处理完成后刷新 `remaining_question_ids`。

### 4. 下一题与 Realtime 自动纠正
- [X] 4.1 从当前 Schedule Task-todo 中按原顺序选择第一条 remaining 必填题。
- [X] 4.2 恢复 instructions 只包含全局护理约束、患者信息和当前这一题。
- [X] 4.3 调用 Qwen `session.update` 后自动 `response.create`。
- [X] 4.4 恢复话术要求先自然纠正“刚才说早了”，再只问当前一题。
- [X] 4.5 恢复模式不在第一条补问后退出；每个患者回答完成 Extraction 后，只更新成下一条 remaining 问题。
- [X] 4.6 remaining 清空后切换为“停止提问、等待系统完成”提示，不重新暴露完整题表。

### 5. 幂等与竞态
- [X] 5.1 同一个已完成 generation 不重复处理 `response.done`。
- [X] 5.2 单会话同时只允许一个 pending recovery task。
- [X] 5.3 患者开口可取消旧恢复推进任务/正在生成的恢复响应，但恢复模式本身继续保持。
- [X] 5.4 患者回答恢复题后等待本轮自动语音响应结束，再触发下一条 `response.create`。
- [X] 5.5 恢复轮标记 `is_recovery`，不递归触发 Guard。
- [X] 5.6 现有 VoiceCompletionCoordinator 保持不变。

### 6. 测试
- [X] 6.1 Guard + VoiceGateway 定向测试第一次：20 passed。
- [X] 6.2 加入患者打断测试后第二次：21 passed。
- [X] 6.3 持续恢复模式定向测试：23 passed。
- [ ] 6.4 临时 GitHub runner 全量测试结果为 266 passed、18 skipped，失败项均集中在 runner 未启动的 Redis 6379 / PostgreSQL 15432 依赖；因此不能替代现有本地完整环境的全量回归。
- [X] 6.5 临时测试 workflow 已删除，最终分支仅保留功能代码、测试与文档。

## 后续可选增强

第一版为了便于调试，结束意图检测采用确定性规则。若真实联调发现模型会使用大量无法覆盖的隐式结束表达，可以把 `VoiceTurnGuard.has_closing_intent()` 换成“规则初筛 + 小模型二分类”；结构化完成判断、Extraction 等待和下一题恢复链路无需改变。


## 主动宣教关闭

- [X] Dialog/Qwen 模型工具列表移除 `get_education_material` 与兼容 `play_audio`。
- [X] Dialog system prompt 移除“吸烟/饮酒必须宣教”、teach-back 和宣教示例。
- [X] 关键词中间件移除抽烟、饮酒、过敏触发宣教工具及戒烟知情同意的逻辑。
- [X] 关键词种子删除 `KW_EDUCATION`，吸烟规则只保留量表事实追问。
- [X] 数据库旧 `trigger_education` 和含宣教指令的 constraint 在运行时过滤。
- [X] Schedule Agent 将 AI 主动健康宣教视为偏离，并移除宣教 Few-shot。
- [X] VoiceGateway 忽略 Redis 中残留的旧宣教 Schedule 指引。
- [X] 应用层即使收到旧会话的 `get_education_material` 调用也返回 disabled，不再读取或展示材料。
- [X] 历史宣教表、事件与前端卡片仅保留回放兼容，不允许产生新的主动宣教。
- [X] 定向回归：59 passed。
