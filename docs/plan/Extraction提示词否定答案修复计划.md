# Extraction 提示词否定答案修复计划

## 1. 提示词调整

- [X] 1.1 强制当前 AI 问句优先，禁止把一句短回答复制到多个未提问字段。
- [X] 1.2 明确“没有/无/否认”等否定表达的布尔和单选映射规则。
- [X] 1.3 明确单选必须返回当前题目真实 `option_code`，不返回展示标签代替编码。
- [X] 1.4 无法确定题目或选项时只进入 `ambiguous_questions`，禁止猜测。

## 2. 验证

- [X] 2.1 增加提示词契约测试。
- [X] 2.2 完成 Python 编译、前端 TypeScript 和差异检查；后端 pytest 因 `.venv` 未安装 pytest 暂无法运行。

## 3. 缺失答案类型容错

- [X] 3.1 模型缺少 `answer_type` 时，按 `question_id` 或 `question_code` 查找量表题目定义并补齐。
- [X] 3.2 无法定位题目时保留人工介入记录，并输出明确的中文原因，禁止暴露裸 `KeyError`。
- [X] 3.3 增加缺失类型的有效补齐和无法定位单元测试。
- [X] 3.4 完成 Python 编译、回退逻辑断言、前端 TypeScript 和差异检查；完整 pytest 因环境未安装 pytest 未执行。
- [X] 3.5 缺失 `answer_type` 且无法定位题目时默认使用 `text`，不再因为缺失类型触发人工介入。
