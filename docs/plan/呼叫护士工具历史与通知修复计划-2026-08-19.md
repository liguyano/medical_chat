# 呼叫护士工具历史与通知修复计划

## 1. 问题确认

- [X] 1.1 确认患者主动呼叫与 Dialog Agent `request_nurse_assistance` 当前共用 `handoff_requested` 领域事件。
- [X] 1.2 确认工具结果目前只作为事件 payload 的部分字段保存，医护对话回放只读取 `interaction_message`，因此看不到工具结果。
- [X] 1.3 确认医护处理接口只更新最新一条未处理呼叫，重复呼叫会留下旧 `pending` 记录。
- [X] 1.4 确认 `handoff_resolved` 未更新历史事件卡片，也未完整携带处理护士工号、姓名和处理时间。

## 2. 后端持久化与事件契约

- [X] 2.1 扩展呼叫请求事件，记录来源、工具名、工具参数和完整工具结果。
- [X] 2.2 扩展呼叫处理事件，记录全部被处理的请求编号、处理护士 ID、工号、姓名、时间和处理说明。
- [X] 2.3 医护处理默认关闭当前任务全部未处理呼叫，避免重复通知刷新后恢复。
- [X] 2.4 历史事件接口返回处理状态和处理人快照，保证患者端与医护端刷新后可恢复。

## 3. 前端状态与组件

- [X] 3.1 区分患者主动呼叫和 Agent 工具呼叫的来源与卡片样式。
- [X] 3.2 完善呼叫卡片：时间、患者、床位、原因、请求操作、工具结果、处理护士工号/姓名/时间。
- [X] 3.3 处理 `handoff_resolved` 时同步关闭同任务全部请求和历史事件卡片。
- [X] 3.4 医护监控对话回放区域渲染历史工具结果，和患者端保持一致。
- [X] 3.5 通知浮层只显示真正未处理的请求，刷新后不再恢复已处理通知。

## 4. 测试与验证

- [X] 4.1 增加后端事件契约和批量处理单元测试。
- [X] 4.2 增加前端 SSE 状态、历史事件和来源样式数据测试。
- [X] 4.3 运行后端相关测试和前端完整测试、Lint、TypeScript。
- [X] 4.4 浏览器验证医护监控回放中的 Agent 呼叫、患者主动呼叫、护士处理结果、工具结果和刷新恢复；患者端因当前浏览器会话无患者登录态未输入敏感身份信息，使用前端事件回归测试与快照恢复逻辑完成验证。

## 5. 审查与交付

- [X] 5.1 更新 backend/frontend 架构约定。
- [X] 5.2 按 Git 规范提交并合并回本地 `main`，删除修复分支。
