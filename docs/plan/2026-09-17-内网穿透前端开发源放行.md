# 内网穿透前端开发源放行计划

## 1. 问题确认

- [X] 1.1 确认 `/_next` 脚本返回 403，页面未完成 React 水合。
- [X] 1.2 确认公网访问主机名为 `lt.frp-gap.com`，并追加 `hohofrontend.cpolar.top`。

## 2. 配置修复

- [X] 2.1 将精确主机名加入 Next.js `allowedDevOrigins`。
- [X] 2.2 运行前端类型检查并确认配置可加载。
- [X] 2.3 记录公网 API 地址仍需满足的访问条件。

## 3. 公网联调说明

- 前端开发服务器必须在配置修改后重启。
- 当前 API 模式地址仍为 `http://localhost:8000`；外部设备登录需要同域反向代理或可公网访问的后端地址。
