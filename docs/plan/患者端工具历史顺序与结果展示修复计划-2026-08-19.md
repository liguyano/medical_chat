# 患者端工具历史顺序与结果展示修复计划

## 1. 问题定位

- [X] 1.1 确认患者端当前将消息、宣教、知情同意和呼叫事件分段渲染，导致工具卡片全部堆到对话底部。
- [X] 1.2 确认医护端工具结果卡片仅展示摘要，患者端展示完整原文/条款，造成两端信息不一致。
- [X] 1.3 确认知情同意状态更新后患者端通过过滤 `signed` 请求移除了历史组件。

## 2. 前端历史与组件修复

- [X] 2.1 新增共享对话历史时间轴模型，按 `occurredAt` 合并普通消息、医学宣教、知情同意和人工介入事件。
- [X] 2.2 患者端改用统一时间轴渲染，保持工具结果位于实际触发位置。
- [X] 2.3 医护端改用完整工具结果只读卡片，展示宣教原文、通俗说明、播报内容和知情同意完整条款。
- [X] 2.4 只读卡片不显示患者操作按钮，但显示患者已阅读/已确认/已签署/拒绝/需解释等结果。
- [X] 2.5 知情同意完成后保留原组件历史，不再过滤掉 `signed` 记录。
- [X] 2.6 保持 SSE 重放和快照恢复幂等，不重复生成历史卡片。

## 3. 测试与验证

- [X] 3.1 增加时间轴排序和完成态保留单元测试。
- [X] 3.2 增加前端事件重放、宣教/知情同意状态更新回归测试。
- [X] 3.3 运行前端测试、TypeScript、Lint、Build 及后端相关测试。
- [X] 3.4 使用任务 109 验证医护端桌面页面顺序和信息一致性；患者端当前浏览器会话无患者登录态，未输入敏感身份信息，使用前端状态回归覆盖患者历史恢复。

## 4. Git 交付

- [X] 4.1 提交修复分支。
- [X] 4.2 合并回本地 `main` 并删除修复分支。
