# 需求1-后端开发计划

## 项目信息
- **需求名称**: 入院量表评估 - AI对话方案
- **需求文档**: [docs/需求1-入院量表评估.md](../需求1-入院量表评估.md)
- **详细设计**: [docs/后端详细设计方案.md](../后端详细设计方案.md)
- **开始时间**: 2026-08-16
- **预计工期**: 2周

---

## 开发步骤

### 1. 基础设施搭建

#### 1.1 数据库初始化
- [X] 执行 Alembic 初始迁移创建批次 A 领域模型（22张表）
- [X] 创建数据库索引优化
- [X] 编写数据库迁移脚本 (Alembic)
- [X] 统一数据库设计事实来源，废弃早期“8张核心表”模型
- [X] 完成 ORM、Alembic 与开发数据库结构一致性校验
- [X] 完成 Alembic 独立临时数据库 upgrade/check/downgrade 可逆性测试
- [ ] 准备测试数据 (fixtures)

#### 1.2 Redis配置
- [X] 配置Redis Stream
- [X] 配置Redis连接池
- [X] 配置Redis过期策略
- [X] 编写Redis工具类 (`backend/app/utils/redis_client.py`)

#### 1.3 Celery配置
- [X] 配置Celery broker和backend
- [X] 配置任务队列路由 (schedule/dialog/extraction)
- [X] 配置Celery Worker启动脚本
- [X] 配置Celery Beat定时任务

#### 1.4 项目配置管理
- [X] 完善 `config.yaml` 配置文件
- [X] 实现配置加载模块 (`backend/app/configs/app_config.py`)
- [X] 配置日志系统
- [X] 配置环境变量管理

**验收标准**:
- 数据库表创建成功，能正常CRUD ✅
- Redis Stream能正常发布订阅 ✅
- Celery Worker能正常启动接收任务 ✅
- 配置文件能正确加载 ✅

---

### 2. Redis Stream通信层实现

#### 2.1 事件发布者
- [X] 实现 `DialogEventPublisher` 类
- [X] 实现事件Schema定义 (`backend/app/schemas/events.py`)
- [X] 实现批量发布功能
- [X] 添加发布失败降级逻辑（标记TODO）

#### 2.2 事件订阅者
- [X] 实现 `EventSubscriber` 基类
- [X] 实现Schedule Agent订阅器
- [X] 实现Extraction Agent订阅器
- [X] 实现SSE推送订阅器

#### 2.3 消息序列化
- [X] 实现消息编解码 (JSON + Pydantic)
- [X] 实现消息版本控制
- [ ] 实现消息压缩 (可选)

**验收标准**:
- 发布消息能被所有订阅者接收 ✅
- 消息格式符合Schema定义 ✅
- 订阅者能正确处理消息 ✅

**单元测试**:
- `tests/unit-test/test_event_publisher.py` ✅ (6/6 passed)
- `tests/unit-test/test_event_subscriber.py` ✅ (5/5 passed)

---

### 3. 智能体状态管理层

#### 3.1 状态管理器
- [X] 实现 `AgentStateManager` 类
- [X] 实现智能体状态序列化 (pickle)
- [X] 实现状态保存到Redis (TTL=1小时)
- [X] 实现状态从Redis恢复
- [X] 明确智能体运行态仅存Redis（不建立废弃的 `agent_states` 表）

#### 3.2 对话历史管理
- [X] 实现 `DialogHistoryManager` 类
- [X] 基于 `interaction_session` / `interaction_message` 实现历史记录保存
- [X] 实现历史记录分页查询、最近消息查询与消息计数
- [X] 实现历史记录格式化 (langchain消息格式)
- [X] 实现会话历史逻辑删除与完整上下文生成

#### 3.3 会话超时管理
- [X] 实现 `SessionTimeoutManager` 类
- [X] 实现患者无响应检测 (5分钟超时)
- [X] 实现会话自动暂停（标记TODO）
- [X] 实现护士通知推送（标记TODO）
- [X] 实现 `TimeoutMonitor` 后台监控器

**验收标准**:
- 智能体状态能正常序列化反序列化 ✅
- Redis故障时记录明确错误，业务消息与评估结果由 PostgreSQL 领域表持久化 ✅
- 超时检测准确触发 ✅

**单元测试**:
- `tests/unit-test/test_agent_state_manager.py`（待补充）
- `tests/unit-test/test_session_timeout.py`（待补充）

**已完成的领域模型测试**:
- `tests/unit-test/test_domain_models.py`：6项通过
- `tests/integ-test/test_domain_persistence.py`：4项通过
- `tests/integ-test/test_alembic_migration.py`：1项通过

---

### 4. Schedule Agent (调度智能体)

#### 4.1 核心逻辑实现
- [X] 实现 `ScheduleAgent` 类
- [X] 实现量表问题列表管理（QuestionTask）
- [X] 实现对话偏离检测算法（LLM语义判断）
- [X] 实现工具调用完整性检查（关键词-工具映射）
- [X] 实现输出结构化Schema (`ScheduleAgentOutput`)
- [X] 实现 `AssessmentQuestionLoader` 量表问题加载器

#### 4.2 提示词工程
- [X] 编写 system_prompt 模板（详细定义偏离判断标准）
- [X] 编写 user_prompt 模板（包含待完成任务列表与对话历史）
- [X] 编写 few-shot 示例（4个场景：追问/偏离/共情/宣教）
- [X] 调优提示词（真实 qwen-plus 7 类场景准确率严格 > 85%）

#### 4.3 Celery任务封装
- [X] 实现 `schedule_agent_worker` 任务
- [X] 实现订阅 dialog_stream 事件循环
- [X] 实现约束事件发布（ConstraintEvent）
- [X] 实现会话结束事件发布（SessionEndEvent）
- [X] 实现轮次计数器Redis持久化（支持任务恢复）
- [ ] 实现任务启动接口（待步骤9 API开发）
- [ ] 实现任务状态查询（待步骤9 API开发）
- [ ] 实现任务停止接口（待步骤9 API开发）

#### 4.4 配置系统扩展
- [X] 新增 `ModelConfig` 模型（支持OpenAI兼容接口）
- [X] 实现 `get_model_config(name)` 与 `get_agent_model_config(agent_name)`
- [X] 使用 `models` 与 `agent_models` 配置 schedule/extraction Agent
- [X] 使用独立 `voice_models` 配置豆包实时语音模型
- [X] 支持 `$ENV` 与 `${ENV}` 密钥引用

**验收标准**:
- 能根据量表编码从数据库加载完整问题列表 ✅
- 偏离检测准确率 > 85% ✅
- 工具调用检查无遗漏 ✅
- 每5轮触发检查 ✅
- 任务支持中断恢复 ✅
- 独立 Celery worker 可初始化 PostgreSQL 与 Redis ✅

**测试结果**（2026-08-17）:

- 单元测试：72 passed
- 集成测试：10 passed
- 真实模型 E2E：1 passed
- 最终统一回归：83 passed
- 步骤4目标模块综合覆盖率：96%
- Windows `solo` Celery worker 真实烟测通过
- 详细记录：`docs/review/步骤4-Schedule-Agent测试报告.md`

---

### 5. Dialog Agent (对话智能体)

#### 5.1 对话引擎抽象层
- [ ] 定义 `DialogEngine` 抽象类
- [ ] 实现 `DoubaoVoiceEngine` (豆包实时语音全双工)
- [ ] 实现WebSocket连接管理
- [ ] 实现音频流处理（PCM/Opus格式）
- [ ] 实现流式事件解析（text/audio/tool_call）

#### 5.2 核心逻辑实现
- [ ] 实现 `DialogAgent` 类
- [ ] 实现上下文管理 (memory)
- [ ] 实现约束提示注入
- [ ] 实现CICARE规则引导
- [ ] 实现工具调用处理

#### 5.3 Middleware实现
- [ ] 实现 `KeywordInterceptMiddleware` (字典库拦截)
- [ ] 实现 `ScheduleConstraintMiddleware` (调度约束注入)
- [ ] 实现 `EventPublishMiddleware` (事件发布)
- [ ] 实现 `TimeoutMiddleware` (超时控制)

#### 5.4 工具定义
- [ ] 实现 `get_education_material` 工具
- [ ] 实现 `trigger_consent_form` 工具
- [ ] 实现 `play_audio` 工具 (预留)
- [ ] 工具Schema定义

#### 5.5 Celery任务封装
- [ ] 实现 `dialog_agent_preheat` 任务 (预热)
- [ ] 实现对话进程管理
- [ ] 实现进程绑定逻辑

**验收标准**:
- 对话流畅自然，符合CICARE规则
- 字典库关键词100%拦截
- 工具调用成功率 > 95%
- 流式输出延迟 < 200ms

**单元测试**:
- `tests/unit/test_dialog_agent.py`
- `tests/unit/test_dialog_middleware.py`
- `tests/unit/test_dialog_tools.py`

---

### 6. Field Extraction Agent (字段抽取智能体)

#### 6.1 核心逻辑实现
- [ ] 实现 `FieldExtractionAgent` 类
- [ ] 实现字段抽取提示词
- [ ] 实现输出Schema定义 (Pydantic)
- [ ] 实现置信度计算

#### 6.2 重试机制
- [ ] 实现最多3次重试
- [ ] 实现结果验证 (格式校验)
- [ ] 实现失败降级 (标记人工补录)

#### 6.3 Celery任务封装
- [ ] 实现 `extraction_agent_worker` 任务
- [ ] 实现订阅dialog_stream
- [ ] 实现抽取结果保存
- [ ] 实现进度推送

**验收标准**:
- 字段抽取准确率 > 90%
- 置信度 < 0.7 的字段标记人工确认
- 抽取延迟 < 3秒

**单元测试**:
- `tests/unit/test_extraction_agent.py`
- `tests/unit/test_extraction_retry.py`

---

### 7. 字典库与关键词匹配

#### 7.1 字典库设计
- [ ] 设计字典库数据结构 (JSON)
- [ ] 编写字典库配置文件 (`backend/app/data/keyword_library.json`)
- [ ] 实现字典库加载模块

#### 7.2 关键词匹配器
- [ ] 实现 `KeywordMatcher` 类
- [ ] 实现正则表达式匹配
- [ ] 实现多关键词同时匹配
- [ ] 实现优先级排序

#### 7.3 API拦截器
- [ ] 在 `send_message` 接口实现拦截
- [ ] 实现约束提示追加逻辑
- [ ] 实现拦截日志记录

**验收标准**:
- 关键词匹配准确率 100%
- 多关键词按优先级排序
- 匹配延迟 < 10ms

**单元测试**:
- `tests/unit/test_keyword_matcher.py`

---

### 8. SSE流式推送服务

#### 8.1 SSE接口实现
- [ ] 实现患者端SSE接口 (`/api/sse/dialog/{session_id}`)
- [ ] 实现医护端SSE接口 (`/api/sse/monitor`)
- [ ] 实现断线重连逻辑 (last_event_id)
- [ ] 实现心跳保活 (30秒一次)

#### 8.2 消息格式化
- [ ] 实现SSE消息格式封装
- [ ] 实现事件类型定义 (dialog_message | progress_update | error)
- [ ] 实现消息ID生成

**验收标准**:
- SSE连接稳定不断线
- 断线重连不丢失消息
- 多客户端同时订阅无冲突

**集成测试**:
- `tests/integration/test_sse_stream.py`

---

### 9. API接口实现

#### 9.1 评估任务管理
- [ ] `POST /api/tasks` - 创建评估任务
- [ ] `GET /api/tasks/{task_id}` - 获取任务详情
- [ ] `POST /api/tasks/{task_id}/cancel` - 取消任务
- [ ] `GET /api/tasks` - 查询任务列表

#### 9.2 对话交互
- [ ] `POST /api/dialog/message` - 发送消息
- [ ] `GET /api/dialog/{session_id}/history` - 获取对话历史
- [ ] `POST /api/dialog/{session_id}/pause` - 暂停对话
- [ ] `POST /api/dialog/{session_id}/resume` - 恢复对话

#### 9.3 字段抽取结果
- [ ] `GET /api/extraction/{session_id}/fields` - 获取抽取字段
- [ ] `POST /api/extraction/fields/{field_id}/confirm` - 人工确认字段

#### 9.4 护士评分
- [ ] `POST /api/rating` - 对消息评分

#### 9.5 宣教与知情同意书
- [ ] `GET /api/education/materials` - 获取宣教材料
- [ ] `GET /api/consent-forms/{form_type}` - 获取知情同意书
- [ ] `POST /api/consent-forms/{form_id}/sign` - 提交签名

**验收标准**:
- 所有接口通过Postman测试
- 返回格式符合API文档
- 错误处理完善 (4xx/5xx)

**集成测试**:
- `tests/integration/test_api_tasks.py`
- `tests/integration/test_api_dialog.py`
- `tests/integration/test_api_extraction.py`

---

### 10. 错误处理与监控

#### 10.1 错误处理
- [ ] 实现全局异常处理器 (FastAPI)
- [ ] 实现模型推理超时处理
- [ ] 实现Redis连接失败降级
- [ ] 实现数据库连接重试

#### 10.2 日志监控
- [ ] 配置结构化日志 (JSON格式)
- [ ] 实现关键指标日志 (推理时长、token消耗)
- [ ] 实现错误日志聚合

#### 10.3 告警机制
- [ ] 实现Celery任务失败告警
- [ ] 实现模型推理失败告警
- [ ] 实现会话超时告警

**验收标准**:
- 异常能被正确捕获并返回友好提示
- 日志格式规范，便于检索
- 关键错误能及时告警

---

### 11. 集成测试

#### 11.1 智能体协作测试
- [ ] 测试 Schedule Agent -> Dialog Agent 协作
- [ ] 测试 Dialog Agent -> Extraction Agent 协作
- [ ] 测试三个智能体完整流程

#### 11.2 Redis Stream测试
- [ ] 测试事件发布订阅
- [ ] 测试多订阅者并发消费
- [ ] 测试消息持久化

#### 11.3 SSE推送测试
- [ ] 测试断线重连
- [ ] 测试多客户端订阅
- [ ] 测试消息顺序性

**测试文件**:
- `tests/integration/test_agent_collaboration.py`
- `tests/integration/test_redis_stream.py`
- `tests/integration/test_sse_stream.py`

---

### 12. 端到端测试

#### 12.1 完整评估流程
- [ ] 模拟医护端创建任务
- [ ] 模拟患者端完整对话
- [ ] 验证字段抽取完整性
- [ ] 验证宣教触发
- [ ] 验证知情同意书签署

#### 12.2 异常场景测试
- [ ] 测试患者中途断线
- [ ] 测试模型推理失败
- [ ] 测试Redis故障
- [ ] 测试数据库故障

**测试文件**:
- `tests/e2e/test_complete_dialog_flow.py`
- `tests/e2e/test_error_scenarios.py`

**验收标准**:
- 完整流程无异常
- 所有字段正确抽取
- 宣教和签署正常触发

---

## 开发顺序建议

按照依赖关系，建议开发顺序如下：

```
1. 基础设施搭建 (1-2天)
   ↓
2. Redis Stream通信层 (1天)
   ↓
3. 智能体状态管理层 (1天)
   ↓
4. Schedule Agent (2天)
   ↓
5. Dialog Agent (3天) [核心]
   ↓
6. Field Extraction Agent (1天) [可并行]
   ↓
7. 字典库与关键词匹配 (0.5天)
   ↓
8. SSE流式推送服务 (1天)
   ↓
9. API接口实现 (2天)
   ↓
10. 错误处理与监控 (1天)
   ↓
11. 集成测试 (1天)
   ↓
12. 端到端测试 (1天)
```

**总计**: 约14个工作日

---

## 风险与应对

| 风险 | 影响 | 应对措施 |
|------|------|----------|
| 模型推理不稳定 | 高 | 增加重试机制，超时降级 |
| Redis Stream消息丢失 | 中 | 消息持久化到数据库 |
| 字段抽取准确率低 | 高 | 优化提示词，增加人工确认 |
| 对话偏离检测不准确 | 中 | 调优Schedule Agent提示词 |
| SSE连接不稳定 | 中 | 心跳保活，断线重连 |
| Celery任务堆积 | 中 | 监控队列长度，动态扩容Worker |

---

## 文档更新计划

开发过程中需要同步更新的文档：

- [ ] API接口文档 (`docs/api/README.md`)
- [ ] 部署手册 (`deploy/Install.md`)
- [ ] 运维手册 (`deploy/Operations.md`)
- [ ] 故障排查手册 (`docs/troubleshooting.md`)
- [ ] 测试报告 (`docs/review/需求1-测试报告.md`)

---

## 当前开发进度记录

### 已完成

- 步骤1：基础设施搭建主体完成；测试数据 fixtures 待准备。
- 步骤2：Redis Stream 通信层完成，事件发布/订阅测试 11 项通过。
- 步骤3：智能体状态管理层完成核心实现；状态管理与超时管理专项测试待补充。
- 步骤4：Schedule Agent 核心模块开发与完整测试完成（2026-08-17）
  - AssessmentQuestionLoader 量表加载器完成
  - ScheduleAgent 核心逻辑与偏离检测完成
  - 提示词模板与 Few-shot 示例完成
  - Celery 任务实现完成
  - LLM 配置系统扩展完成
  - 72 项单元测试、10 项集成测试、1 项真实模型 E2E 全部通过
  - 步骤4目标模块综合覆盖率 96%
  - 真实 Windows `solo` Celery worker 烟测通过
- 需求1批次 A 数据库完成：22 张领域表、ORM、Alembic 初始迁移及
  `DialogHistoryManager` 持久化实现均已落地。

### 批次 A 自动化验证（2026-08-17）

- 完整单元测试与 PostgreSQL 领域持久化测试：21 项通过。
- Alembic 独立临时数据库迁移测试：1 项通过，临时数据库已自动清理。
- 开发数据库版本：`26533d4669bd (head)`。
- `alembic check`：ORM 与运行数据库无结构差异。
- 开发库保持 22 张业务表，集成测试使用外层事务回滚，未残留测试数据。
- 批次 A 新增 ORM、迁移、管理器与测试文件通过 Ruff 静态检查。

### 相关提交

- `253cd51 docs(backend-design): 统一数据库设计事实来源,废弃8张核心表旧模型`
- `3417577 refactor(backend-db): 按领域模型重建需求1批次A ORM与迁移`
- `bb16881 test(backend-db): 补充批次A ORM与迁移自动化测试`
- `8a699fe feat(backend-agent): 完成Schedule Agent核心模块开发`

### 下一步

- 步骤4测试分支审查通过后合并回本地 `main`。
- 按用户要求，本轮不开展步骤5 Dialog Agent。
- 后续工作等待用户指令。

---

## 完成标准

开发完成需满足以下标准：

- [ ] 所有单元测试通过且覆盖率 > 80%（当前已执行测试通过，覆盖率尚未核定）
- [ ] 所有集成测试通过
- [ ] 所有E2E测试通过
- [ ] 代码审查通过 (Code Review)
- [ ] 性能测试通过 (响应时间、并发量)
- [ ] 安全审查通过 (SQL注入、XSS等)
- [ ] 文档完整且准确

---

**创建时间**: 2026-08-16  
**最后更新**: 2026-08-17
**负责人**: AI开发助手
