# 医护监控布局与结构化答案真实值修复计划

日期：2026-08-19

## 1. 需求分析与数据链路确认

- [X] 1.1 阅读仓库、后端、前端及测试模块开发规范。
- [X] 1.2 确认结构化答案当前由 `AssessmentAnswerOption.option_code_snapshot` 返回，导致页面显示 `option_3` 等编码。
- [X] 1.3 确认患者端和医护端共用 `StructuredAnswer` 映射，需在 DTO、SSE、快照和页面展示边界统一真实量表值。
- [X] 1.4 对照原型图确认医护监控页需要顶部患者摘要、左侧任务概览/结构化答案、中间对话回放、右侧 AI 质评及对话下方呼叫提示栏。

## 2. 后端真实量表值输出

- [X] 2.1 扩展抽取字段 Schema，返回选项编码、标签和值的可审计快照及统一展示值。
- [X] 2.2 修改抽取查询，同时读取 `AssessmentOption.option_label/option_value`，优先返回目标量表真实值。
- [X] 2.3 确保 `extraction_updated` SSE 与历史快照使用同一字段结构。
- [X] 2.4 增加后端单元测试，覆盖单选、多选、文本/数值答案、可信度和证据消息 ID。

## 3. 前端结构化答案展示

- [X] 3.1 扩展 DTO、领域类型、映射器和 SSE 事件适配，保留原始编码但以真实量表值作为用户可见值。
- [X] 3.2 患者端已记录信息和医护端结构化答案统一渲染真实值，禁止用户可见区域出现 `option_*` 编码。
- [X] 3.3 保留可信度标签、证据消息和纠正能力。
- [X] 3.4 增加前端映射及展示回归测试。

## 4. 医护监控页原型布局

- [X] 4.1 增加顶部患者信息摘要：姓名/在院状态、住院号、床位、科室、性别年龄、入院日期。
- [X] 4.2 按原型重排左侧任务概览、目标量表状态、结构化答案；结构化答案使用真实量表值。
- [X] 4.3 保持中间对话时间轴中的普通消息、宣教、知情同意和呼叫工具结果，不改成摘要。
- [X] 4.4 保持右侧本轮 AI 质评交互和自由评价能力。
- [X] 4.5 在中间对话区底部增加当前未处理呼叫提示栏，区分患者主动呼叫与 AI 工具呼叫；处理后提示栏消失，历史仍保留。

## 5. 验证、审查与合并

- [X] 5.1 运行后端测试、前端 Vitest、TypeScript、Lint 和 Build。
- [X] 5.2 使用任务 109 的真实医护页面验证桌面端，并完成 390px 响应式规则与构建检查。
- [X] 5.3 记录 review 与 debugger 结果，修复回归问题。
- [X] 5.4 提交中文 Git 说明，合并回本地 `main` 并删除修复分支；不推送远程。
