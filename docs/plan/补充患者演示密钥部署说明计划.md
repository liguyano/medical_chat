# 补充患者演示密钥部署说明计划

## 需求目标

在 `deploy/Install.md` 中明确真实演示数据恢复时，
`PATIENT_IDENTITY_SECRET` 必须与导出数据时保持一致，避免医护端可登录而患者端
身份证号校验失败。

## 开发步骤

- [X] 1.1 记录问题原因：身份证密文依赖 `PATIENT_IDENTITY_SECRET`。
- [X] 1.2 补充服务器检查、修复和重启命令。
- [X] 1.3 说明该密钥不能随意更换，真实数据迁移后更换会导致患者登录失败。
- [ ] 1.4 完成文档检查并合并本地 `main`。
