# 本地模型连接配置计划

## 1. 模型端点配置

- [X] 将本地 `config.yaml` 中两个语言模型切换到指定的阿里云百炼工作空间端点。
- [X] 按用户要求将模型密钥直接写入本地且已被 Git 忽略的 `config.yaml`。
- [X] 将实时语音 WebSocket 地址替换为当前阿里云百炼工作空间地址。

## 2. 运行说明

- [X] 说明 API、Celery Worker 和 Beat 需在设置环境变量后重启。
- [X] 说明已公开密钥需要撤销并更换。
