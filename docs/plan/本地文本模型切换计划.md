# 本地文本模型切换计划

## 1. 配置调整

- [X] 将普通文本模型的实际调用模型切换为 `qwen3.8-max`。
- [X] 将 Schedule 低温度模型的实际调用模型切换为 `qwen3.8-max`。
- [X] 保留现有 Agent 绑定、温度参数和实时语音模型。

## 2. 验证与运行

- [X] 检查两个文本模型条目的实际模型名称。
- [X] 说明重启 API 和 Celery Worker 后生效。
