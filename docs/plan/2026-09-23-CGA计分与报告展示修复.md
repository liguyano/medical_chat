# CGA 量表得分及报告展示修复

## 目标与边界

从已合并 CGA 的 main 新建 `fix/cga-report-score-recalculation`，不修改 main、也不触碰已有同方向分支。

当前问题：AI 对话选项分数写入 `assessment_answer_option.clinical_score`，总分仅累加 `assessment_answer.clinical_score`，选项为 None 时被当成零；报告还拼接了相同的提交解释和分数解释，且缺分误写成零。修复应以实际持久化选项快照为事实，不能用 AI 摘要推断分数。

## 1. 分析
- [x] 1.1 检查 main 当前 CGA 页面、AI 抽取/计分、提交模型及报告历史。
- [x] 1.2 区分真实零分、尚缺必填答案、缺少选项分数及没有计分项；缺少依据不得生成零分。
- [x] 1.3 当前最终确认提交为护士纯文本答案，借用同量表旧 AI 分数须核对计分题答案，不得覆盖护士修正。

## 2. 代码
- [x] 2.1 后端累计真实已选项分数和非选择题已有临床分数，回写每题分数供明细展示。
- [x] 2.2 必填题缺失、计分项未答或缺失计分依据时清空旧总分，未知风险等级不得套跨量表通用阈值。
- [x] 2.3 重新生成报告时重算已有 AI 选项分数，不改旧报告版本；只在计分答案一致时沿用其他来源分数。
- [x] 2.4 后端快照传递计分状态，前端“未完成计分”“未进行相关量表”区分展示。
- [x] 2.5 去重前端结果解释，保留真实的 0 分以及原始详细答案。

## 3. 测试与交付
- [x] 3.1 覆盖单选、多选、文本/数值、真实零分、部分回答、空选项及风险规则的单元测试。
- [x] 3.2 覆盖重复解释、缺失得分、已完成零分、未执行量表的前端测试。
- [ ] 3.3 更新 backend/frontend AGENTS 与审查说明，尝试运行测试及类型检查。
- [x] 3.4 核对最终 diff，仅保留修复分支；不合并 main、不导入真实患者数据。

## 历史报告

已有报告 `source_snapshot` 是不可变版本；部署修复后，需要护士点击“重新生成”得到新版本才能看到更新后的计分。任何无法确定是否同源的历史分数显示“未完成计分”，不得凭空推算。


## 本轮执行结果
- 已提交计分重算、选项分数回写、有效计分状态、护士确认答案同源性核对、旧报告保留及前端重复解释去重；并补充了前后端单元测试。
- 代码复核时补齐了“计分题为选填但尚未回答”场景的缺分保护，以及前端测试中睡眠量表缺分的预期。
- 独立 Python/TypeScript 冒烟逻辑检查通过；完整仓库的 pytest、Vitest 和类型检查因本环境无法克隆 GitHub 项目而尚未执行，因此 3.3 保留待完成。
- 审查和本地验收步骤记录在 docs/review/2026-09-23-CGA选项计分与报告展示修复审查.md；不会自动修改或部署线上任务 84。
