# 医护端配置、护理计划与宣教确认修复开发计划

## 一、需求边界

- [X] 1.1 宣教材料、拦截特征字典和评估量表用于 Demo 系统管理，采用登录医护直接查看、原地修改、立即生效。
- [X] 1.2 配置中心不建设草稿、审批、发布和操作审计流程。
- [X] 1.3 配置保存保留必要的必填项、数据类型、正则表达式和关联完整性校验。
- [X] 1.4 患者画像与护理计划属于医护业务结果，AI 生成内容保持草案状态，由护士编辑并确认。
- [X] 1.5 本次必须完成真实后端接口、前端 API/Mock 仓储适配、自动化测试和浏览器联调。

## 二、宣教确认 BUG 修复

- [X] 2.1 分离领域事件 ID 与 Redis Stream 游标 ID，SSE 事件同时携带 `event_id` 和 `stream_id`。
- [X] 2.2 前端断线续读只使用 `stream_id`，领域卡片和业务请求只使用 `event_id`。
- [X] 2.3 API 快照恢复时替换当前任务的宣教、知情同意和呼叫事件集合，清除陈旧 `sessionStorage` 数据。
- [X] 2.4 宣教确认接口增加重复提交幂等处理，同时保留任务、患者、材料和来源事件校验。
- [X] 2.5 补充后端 SSE/宣教确认测试和前端事件解析、快照恢复测试。
- [X] 2.6 使用任务 112 验证“我已阅读并了解”可成功确认且刷新后状态可恢复。
- [X] 2.7 编写 BUG 记录和代码审查记录。

## 三、简化配置中心

- [ ] 3.1 为宣教方案、版本和内容单元补充 ORM 与 Alembic 迁移，并导入现有四类内置宣教材料。
- [ ] 3.2 将文本对话和实时语音的宣教工具统一切换为数据库材料查询，保留事件内容快照。
- [ ] 3.3 实现宣教材料列表、详情和直接更新接口。
- [ ] 3.4 实现拦截特征字典列表、详情、直接更新、启停和测试命中接口。
- [ ] 3.5 实现跨 API/Celery Worker 的拦截字典缓存刷新，配置保存后立即生效。
- [ ] 3.6 实现量表列表、完整配置详情和直接更新接口，覆盖分组、题目、选项、规则与护理措施。
- [ ] 3.7 重构医护端配置页为宣教材料、拦截特征字典、评估量表三个管理页签。
- [ ] 3.8 补充 API/Mock 仓储、DTO 映射、表单校验和错误反馈。
- [ ] 3.9 完成后端单元/集成测试、前端 Vitest、lint、typecheck 和 build。
- [ ] 3.10 完成桌面端与 390px 手机视图浏览器验证，并检查控制台无错误。
- [ ] 3.11 更新架构说明和代码审查记录。

## 四、患者画像与护理计划

- [ ] 4.1 按数据库事实来源补充 `patient_profile_snapshot`、`nursing_plan`、`nursing_plan_item` ORM 与迁移。
- [ ] 4.2 补齐护士评估复核后端接口，保存护士提交、最终提交和差异原因。
- [ ] 4.3 聚合任务量表、结构化答案、得分、风险标签和对话完成度，生成带来源证据的患者画像。
- [ ] 4.4 配置并实现护理计划生成服务，输出风险摘要、宣教摘要、交接班摘要和结构化计划项。
- [ ] 4.5 任务进入 `pending_review` 后异步幂等生成 AI 草案，失败时记录错误并支持重试。
- [ ] 4.6 护士确认最终评估后根据最终提交刷新患者画像与护理计划。
- [ ] 4.7 实现护理计划查询、重新生成、编辑计划项、接受/修改/拒绝和最终确认接口。
- [ ] 4.8 新增医护端患者画像与护理计划页面，并接入任务详情和复核流程。
- [ ] 4.9 Mock/API 两种模式保持同一仓储接口和可演示流程。
- [ ] 4.10 完成规则聚合、模型结构化输出、异步重试、接口和前端交互测试。
- [ ] 4.11 使用真实模型和真实 PostgreSQL/Redis 完成可复现 E2E 联调。
- [ ] 4.12 完成桌面端与 390px 手机视图浏览器验证，并检查控制台无错误。
- [ ] 4.13 更新部署说明、架构说明和代码审查记录。

## 五、交付与 Git

- [ ] 5.1 每个功能分支从本地 `main` 创建，测试和审查完成后合并回本地 `main` 并删除分支。
- [ ] 5.2 提交信息使用中文摘要和仓库规定的作用域前缀。
- [ ] 5.3 合并前确认迁移头唯一、数据库升级成功、自动化测试通过。
- [ ] 5.4 全量回归后确认本地 `main` 状态和工作区干净。
- [ ] 5.5 任务结束前停止本次联调启动的前后端进程，避免占用 IDE 调试端口。
