# 桌面对话进度浏览器验证

日期：2026-09-03。

使用已有 Playwright 和系统 Edge 无头浏览器，运行真实 Next.js 页面、组件及 CSS。
在 `.dev/runtime/dialogue-preview` 创建隔离源码副本，以 Mock 模式使用 3107 端口；
未复用或修改用户已运行的 3000 端口服务。截图使用人工构造的演示会话：年龄已记录、
身高已问且为当前题，其他题未询问，不含真实患者资料。

## 验证结果

- 桌面 1440×1000：侧栏宽 360px，聊天区域宽 640px；页面高度为 1000px。
- 题目列表可视高度 785px、内容高度 1231px；滚动列表 250px 时，整页位置保持不变。
- 390×844 手机：侧栏隐藏，画布和页面宽度均为 390px，无横向溢出，保留原聊天交互。
- 普通患者任务页在桌面仍为 430px 画布，没有挂载侧栏。
- 当前题高亮、三种状态、按量表分组及顶部/侧栏一致的 1/11 进度均可见。
- 浏览器断言全部通过；前端定向 8 文件 27 测试、类型检查及改动页面 ESLint 通过。

本轮发现并修复：桌面对话沿用手机高度造成页面多出 34px 滚动；顶部旧计数与侧栏快照不一致。
桌面高度现在扣除外框边距和边框，顶部优先消费同一题目快照。

没有发现应用运行时异常。现有全局 CSS 引用的 Google Fonts 请求被本机网络策略拒绝，
控制台有对应资源加载错误；截图使用本地回退字体。本次未修改该既有字体依赖，也未验证真实
后端、模型或语音服务。

## 证据与清理

- `dialog-question-progress-desktop.png`：桌面截图。
- `dialog-question-progress-mobile.png`：手机截图。
- `dialog-question-progress-browser.json`：尺寸、滚动及请求失败记录。

已停止本次启动的隔离 Next.js 进程及子进程。检查确认 3107 无监听、3000 原服务仍在，
没有遗留 Playwright 临时浏览器进程。验证后已清理本次临时源码、构建输出和执行脚本；
先单独移除指向真实 `node_modules`、`public` 的目录联接，再删除临时目录，原依赖及素材保留。
