# 患者画像与护理计划前后端代码审查

## 一、审查范围

- 后端患者画像、护理计划、护理计划明细 ORM 与 Alembic 迁移。
- 护士评估复核、护理计划查询、生成、编辑和确认接口。
- 评估完成后的 Celery 自动生成派发与真实模型结构化输出。
- 前端 API/Mock 仓储、任务详情入口及患者画像与护理计划页面。

## 二、审查结论

- [X] 数据表与字段符合 `docs/sql/数据库表业务设计.md` 第 12 节。
- [X] AI 结果以 `ai_draft` 保存，护士处理全部建议并确认后才转为 `confirmed`。
- [X] 来源提交按最终确认、护士独立、AI 抽取顺序选择，最终复核会触发强制刷新。
- [X] 模型输出关闭 thinking，并通过 Pydantic JSON Schema 校验。
- [X] API 与 Mock 模式实现同一生成、编辑、处置、确认流程。
- [X] 医护端生成请求使用 120 秒独立超时，覆盖真实模型耗时。
- [X] 已确认计划前端只读，后端拒绝直接修改；确认前禁止遗留待处理项或全部拒绝。
- [X] 任务归属校验覆盖护理计划查询、生成、编辑和确认接口。

## 三、验证记录

- 后端 Ruff 检查通过。
- 后端单元测试 218 项通过。
- 前端 Vitest 20 个文件、60 项通过。
- 前端 lint、typecheck、生产 build 通过。
- Alembic 唯一 head/current：`20260820_nursing_plan`。
- 使用真实 PostgreSQL、Redis 和 Qwen 模型对任务 111 完成生成：
  - 生成患者画像、风险摘要、宣教重点、交接班重点和 5 条护理建议。
  - 护士修改 1 条、接受 4 条并成功保存。
  - 最终确认后计划状态为 `confirmed`，页面切换为只读。
- 桌面端和 390×844 视图通过，根页面宽度为 390px，无横向溢出，浏览器控制台无错误。

## 四、剩余风险

- 自动生成依赖 `extraction_queue` 对应 Celery Worker 运行；Worker 未启动时页面仍可通过
  同步生成按钮完成演示。
- 护理计划属于辅助建议，真实临床使用前仍需由医院补充专业审核、权限和合规流程；本次
  按 Demo 范围不建设审批发布和全程审计。
