# 前端 Mock/API 双适配与实时传输代码审查

> 审查日期：2026-08-16
>
> 分支：`feat/frontend-backend-integration`
>
> 结论：Mock 原型可用，前端联调基础通过；真实 API 与语音验收等待后端完成。

## 1. 审查范围

- [X] 1.1 运行时模式与环境变量。
- [X] 1.2 HTTP Client、DTO、领域模型映射和 Repository。
- [X] 1.3 患者与护士 SSE 事件消费。
- [X] 1.4 浏览器到后端的语音 WebSocket、PCM16 和播放队列。
- [X] 1.5 任务创建、传统问卷、AI对话、监控、复核、质评和知情同意页面。
- [X] 1.6 单元测试、静态检查、生产构建和浏览器回归。

## 2. 审查结果

- [X] 2.1 默认 `mock`，后端未启动时不会阻断原型。
- [X] 2.2 页面未直接调用 `fetch`、`EventSource` 或 WebSocket。
- [X] 2.3 REST 仅提交命令；文本、状态和护士监控由 SSE 接收。
- [X] 2.4 患者实时语音仅使用 WebSocket，失败后保留文字输入。
- [X] 2.5 Float32 音频经过限幅、降采样和 Int16 量化，不直接发送底层字节。
- [X] 2.6 SSE 使用事件 ID 去重，并携带 `last_event_id` 重连。
- [X] 2.7 API 操作失败时显示错误，不将本地 Store 修改伪装成后端成功。
- [X] 2.8 Mock/API、SSE 和语音状态沿用现有 Badge、颜色和卡片风格。

## 3. 验证记录

- [X] 3.1 `pnpm test`：6 个测试文件、11 项测试通过。
- [X] 3.2 `pnpm lint`：通过。
- [X] 3.3 `pnpm typecheck`：通过。
- [X] 3.4 `pnpm build`：Next.js 16.3.1 Turbopack 生产构建通过。
- [X] 3.5 桌面浏览器：护士登录、任务创建、患者对话、监控和反馈通过。
- [X] 3.6 390px 浏览器：患者对话和知情同意无横向溢出。
- [X] 3.7 知情同意：逐条播放/确认、Canvas签名和完成页跳转通过。
- [X] 3.8 浏览器控制台：未发现应用错误或警告。

## 4. 等待后端完成的联调项

- [ ] 4.1 根据最终 OpenAPI 修正患者、量表、任务和会话 DTO。
- [ ] 4.2 确认 `pending_review` 或独立复核状态字段。
- [ ] 4.3 确认所有 SSE `event_type` 和 `payload` Schema。
- [ ] 4.4 确认 HttpOnly Cookie 鉴权、CORS 和 SSE 重连行为。
- [ ] 4.5 确认语音 WebSocket 路径、控制消息、PCM采样率、服务端关闭码和音频序号。
- [ ] 4.6 验证真实麦克风权限、语音转录、AI音频播放、打断与文字降级。
- [ ] 4.7 完成任务、对话、监控、复核、质评和签名的真实后端闭环。

## 5. 合并约束

- [X] 5.1 当前改动只保存在前端联调 worktree。
- [X] 5.2 未修改主工作区中的后端开发文件。
- [ ] 5.3 后端提交并完成真实联调前，不合并回 `main`。
